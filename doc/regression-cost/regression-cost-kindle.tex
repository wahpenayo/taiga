% !TEX TS-program = arara
% arara: xelatex: { synctex: on, options: [-halt-on-error] } 
% arara: biber
% skip-arara: makeglossaries
% skip-arara: makeindex
% arara: xelatex: { synctex: on, options: [-halt-on-error]  } 
% arara: xelatex: { synctex: on, options: [-halt-on-error]   } 
% arara: clean: { files: [regression-cost-kindle.aux] }
% arara: clean: { files: [regression-cost-kindle.bbl] }
% arara: clean: { files: [regression-cost-kindle.bcf] } 
% arara: clean: { files: [regression-cost-kindle.blg] } 
% arara: clean: { files: [regression-cost-kindle.glg] }
% arara: clean: { files: [regression-cost-kindle.glo] }
% arara: clean: { files: [regression-cost-kindle.gls] }
% arara: clean: { files: [regression-cost-kindle.idx] }
% arara: clean: { files: [regression-cost-kindle.ilg] }
% arara: clean: { files: [regression-cost-kindle.ind] }
% arara: clean: { files: [regression-cost-kindle.loe] }
% arara: clean: { files: [regression-cost-kindle.lof] }
% arara: clean: { files: [regression-cost-kindle.log] }
% arara: clean: { files: [regression-cost-kindle.lol ] }
% arara: clean: { files: [regression-cost-kindle.out] }
% arara: clean: { files: [regression-cost-kindle.run.xml] }
% arara: clean: { files: [regression-cost-kindle.toc] }
% arara: clean: { files: [regression-cost-kindle.xdy] }
% arara: clean: { files: [regression-cost-kindle.synctex.gz] }
%-----------------------------------------------------------------
\documentclass[10pt,openany]{article}
\input{../tex/head}
\input{../tex/kindle-paperwhite}
\input{../tex/clj-listings}
\input{../tex/symbols}

\input{regression-cost-body}
