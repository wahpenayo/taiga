% !TEX TS-program = arara
% arara: xelatex: { synctex: on, options: '-halt-on-error' } 
% arara: biber
% skip-arara: makeglossaries
% skip-arara: makeindex
% arara: xelatex: { synctex: on, options: '-halt-on-error'  } 
% arara: xelatex: { synctex: on, options: '-halt-on-error'   } 
% arara: clean: { files: [regression-cost.aux,regression-cost-kindle.bbl] }
% arara: clean: { files: [regression-cost-kindle.bcf,regression-cost-kindle.blg] }
% arara: clean: { files: [regression-cost-kindle.glg,regression-cost-kindle.glo] }
% arara: clean: { files: [regression-cost-kindle.gls,regression-cost-kindle.gls] }
% arara: clean: { files: [regression-cost-kindle.idx,regression-cost-kindle.ilg] }
% arara: clean: { files: [regression-cost-kindle.ind,regression-cost-kindle.loe] }
% arara: clean: { files: [regression-cost-kindle.lof,regression-cost-kindle.log ] }
% arara: clean: { files: [regression-cost-kindle.log,regression-cost-kindle.lol ] }
% arara: clean: { files: [regression-cost-kindle.out ] }
% arara: clean: { files: [regression-cost-kindle.run.xml] }
% arara: clean: { files: [regression-cost-kindle.toc,regression-cost-kindle.xdy] }
% arara: clean: { files: [regression-cost-kindle.synctex.gz] }
%-----------------------------------------------------------------
\documentclass[10pt,openany]{article}
\input{../tex/head}
\input{../tex/kindle-paperwhite-header}
\input{../tex/clj-listings}
\input{../tex/symbols}

\input{regression-cost-body}
